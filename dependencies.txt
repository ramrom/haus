WEMO - get ouimeaux API package
  - pip install ouimeaux
speech synth
  - sudo apt-get install espeak python-espeak
speech recog - use CMUSphynx
spotify:
  - mopidy (apt-get, need gpg key)
  - mopidy-spotify (apt-get)
  - moped (pip install Mopidy-Moped)
